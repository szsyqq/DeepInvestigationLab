# Skills 和工作流 · 体检报告

日期：2026-07-22 | 诊断对象：`~/.workbuddy/skills/`（用户级 16 个技能）+ 项目工作流文件

> 你昨天（7月22日）确立了一套三层的书写准则（Skill / Workflow / Script）。这份报告先体检目前有什么问题，然后给你几种解决思路，你来选方向。

---

## 一、先说结论：问题确实有，但不严重

现在 "skills" 和 "工作流" 的概念有点**混在一起**了——有些文件该放在技能里的内容塞到了工作流里，该在工作流里的东西塞到了技能里，还有一些技能之间**做的事差不多但各自写了一套**。

好比一个工具箱里：螺丝刀和扳手没分清楚，三把钳子长得差不多但没人记得哪把是剪电线的、哪把是夹管子的。

---

## 二、具体发现了哪些问题 👇

### 问题 1：套路写死了，换个项目用不了

**说人话**：几个技能的路径是写死的我的电脑用户名（`panyp`），换台机器或者别人来用，直接报错。

| 文件 | 写死的路径 |
|------|-----------|
| `link-inbox` | `/Users/panyp/文稿-本地-不参与同步/# WorkBuddy/#LittleRedBook/...` |
| `gx`（路由壳） | 同上 |
| `jc`（路由壳） | 同上 |
| `sl`（路由壳） | 同上 |

**严重程度**：⭐⭐⭐（最高）—— 这些技能完全绑定到你的一台电脑，跨项目、跨机器不可用。

### 问题 2：沙箱链接写死了，换一次部署就失效

**说人话**：`报道流程.md` 里写死了一个 CloudStudio 沙箱链接（`https://f3ae6e55176049c2a6c9853743472037.app.codebuddy.work`），如果以后换部署环境，这个链接就要手动改。

**严重程度**：⭐⭐⭐（对于这个调查报道项目影响大）

### 问题 3：工作量太不均——最大的文件 > 1100 行

| 文件 | 行数 | 感觉 |
|------|:----:|------|
| `pdfkit-py` | **1105 行** | 像一本说明书塞进了一个技能里 |
| `llm-wiki-skill` | **909 行** | 也快赶上小册子了 |
| `wsj-investigative-report` | 97 行 | ✅ 合理 |
| `gx/jc/sl` 路由壳 | ~25 行 | ✅ 合理（但太瘦了） |

**pdfkit-py** 的问题特别明显——里面塞了 100 多个 bash 命令示例、完整的 Python 代码模板。按 7 月 22 日立的规矩（技能文件不超过 300 行），它超了 3 倍多。

### 问题 4：三个技能做同一件事——浏览器上网

三个技能功能高度重叠：

| 技能 | 描述 | 核心理念 |
|------|------|---------|
| `web-access` | 用 CDP 连接本地 Chrome 做上网操作 | 基于 Chrome DevTools 协议 |
| `browser-use` | 浏览器自动化，测试/填表/截图 | 基于 browser-use 的 Python 库 |
| `agent-browser-core` | 浏览器交互，快照 + 结构化输出 | 基于 Node.js/Rust 的 CLI 工具 |

**严重程度**：⭐⭐——虽然每个用不同的底层工具，但对用户（就是你）来说，你不太好判断"这次用哪个"，我也容易选错。

### 问题 5：边界模糊——技能和项目的"私事"混在一起

**说人话**：通用技能（所有人都能用的方法论）里，塞了一些**只有这个项目才用的设置**。

典型例子：`wsj-investigative-report` 技能（写作方法论）的第 26-29 行，写了「宇树标题特例」和「小红书标题特例」。如果以后写其他公司的报道，这些特例不应该出现在技能文件里。

同理，`gx/jc/sl` 三个技能的用户名和路径完全绑定了「小红书知识库」这一个项目，但它们的技能描述写得很"通用"的样子——这个矛盾会让未来困惑。

### 问题 6：版本历史不该在正式文件里

`报道流程.md` 最后有一段版本历史（v1~v5），记录了从 7 月 17 日到 7 月 21 日的迭代过程。这在项目起步阶段有道理，但现在版本历史已经是**历史信息**了——对执行具体工作没帮助，反而让文件变长、分散注意力。

### 问题 7：link-inbox 和 sl 功能重复

`link-inbox` 和 `sl` 做的事差不多：收到链接 → 判断来源 → 归档到 inbox。区别只在于：

- `link-inbox`：通用归档，目标目录是 Obsidian vault 的 `raw/inbox/`
- `sl`：仅适用于小红书知识库的 `raw/inbox/`

**严重程度**：⭐ — 虽然有区别，但用户（你）确实可能困惑"该用哪个"。

---

## 三、关于 7 月 22 日新立的规矩

昨天（今天其实是同一天）你确立了三个层的概念：

| 层 | 是什么 | 主要规则 |
|:---|:------|:---------|
| **层1: Skill** | 跨项目复用的方法论、算法、原则 | ≤300 行，无硬编码路径，无完整命令 |
| **层2: Workflow** | 项目特有操作流程 | 步骤序列 + 路径约定 + 部署方式 |
| **层3: Script** | 具体执行的 `.sh`/`.py` | 可执行命令 |

**现状与规矩的差距**：目前的文件是在这个规矩之前写的，所以没有遵守也很正常。体检结果就是帮我们看看哪些地方要改。

---

## 四、解决思路（你来选方向）

下面给你 **3 种方案**，从"温和调整"到"彻底重构"。每种说清楚：要做什么、工作量多大、好处和代价。

---

### 方案 A：按现有规矩做小手术 🩹

**思路**：不动大结构，只把明确违规的地方修好。

具体要做的事：
1. **凡是有 `/Users/panyp/文稿-本地...` 的技能** → 把路径改成变量（比如 `$KNOWLEDGE_BASE_PATH`），在 `config.env` 或项目的 `_workflow` 里指定一次
2. **三个浏览器上网技能** → 整理一份「查询指引」告诉我什么场景调用哪个
3. **pdfkit-py** → 把它的大块 bash 例子和 Python 模板拆到 `scripts/` 目录，skill 本身只留关键规则（控制在 ≤300 行）
4. **llm-wiki-skill** → 同上，把过长的输出模板拆到 `scripts/` 或 `references/` 目录
5. **报道流程.md** → 去掉版本历史（移到一个历史归档文件）；沙箱链接改成占位符，部署时动态填
6. **wsj-investigative-report** → 把宇树/小红书标题特例移到项目 `_workflow` 文件
7. **gx/jc/sl** → 路径改成变量

**工作量**：中等（约 2-3 小时，主要是拆文件 + 建变量系统）
**好处**：不改变现有使用习惯，改完后就能用
**代价**：路径变量系统需要自己维护一个 `config.env`；三个浏览器技能还是会有点混乱

---

### 方案 B：正规化重组 ✂️📁

**思路**：严格按照 Skill / Workflow / Script 三层规则，重新分配每个文件的内容。

具体要做的事（包括方案 A 的所有步骤 + 额外）：
1. 明确每个技能的角色，消除重叠：
   - **三个浏览器技能**：合并成一个「browser-automation」技能，内部用"场景分支"来决定用哪种工具。或者保留三个但给每个加上非常清晰的触发条件（"用什么工具解决什么问题"），写在每个 skill 的 frontmatter 里
   - **link-inbox vs sl**：合并成一个通用 `link-archive` 技能，通过参数指定目标目录（小红书 vs 通用）
2. 每个技能的 `scripts/` 目录放可执行脚本，`SKILL.md` 只保留路由 + 核心规则
3. `_workflow/` 文件只放项目特有路径和部署方式，不重复技能里的内容
4. 所有硬编码路径换成环境变量，统一读取
5. 技能文件全部控制在 300 行以内

**工作量**：较大（约半天到一天，需要重新规划分类标准 + 动手改）
**好处**：结构清晰，以后加新项目不会乱，也更容易迁移
**代价**：改的时候旧引用可能需要兼容处理；写的过程中暂停其他工作

---

### 方案 C：先体检不改 🧐

**思路**：承认现在有这些毛病，但不急着改。先把这份报告存下来，等到下次遇到实际问题再说。

具体做法：
1. 这份报告存档到 `_diagnosis/` 目录
2. 日常使用中留意：如果某个技能频繁出错（比如路径失效、用错浏览器技能），那时再针对性修
3. 新写的技能和流程严格按 7 月 22 日规矩来

**工作量**：极小（今天不用改任何东西）
**好处**：不中断你现在在做的事情（调查报道项目正在推进中）
**代价**：问题不会自己消失，以后可能会遇到"这个技能怎么不能用"的坑

---

### 小结对比

| 维度 | 方案 A：小手术 | 方案 B：正规化重组 | 方案 C：先放着 |
|:----|:------------:|:---------------:|:------------:|
| 需要的时间 | 2-3 小时 | 半天到一天 | 0 |
| 能解决的最大问题 | 路径写死 + pdfkit 太胖 | 全部 7 个问题 | 无 |
| 对当前工作的影响 | 需要暂停半天 | 需要暂停 1-2 天 | 不影响 |
| 长期维护负担 | 降低一些 | 大幅降低 | 不变 |

---

**你倾向哪个方向？** 告诉我后我再出后续的详细执行计划。

你也可以混合选——比如"先做方案 A，但把三个浏览器技能放到方案 B 再处理"，或者"路径问题先修，其他往后放"。
